# 🤖 BOT COORDINATOR — Архитектура виртуального ассистента-делегатора

## 📌 Назначение
Главный бот выступает как **ассистент руководителя**, получающий задачи от пользователя, интерпретирующий их с помощью LLM и делегирующий выполнение специализированным суб-ботам по компетенциям.

## ⚙️ Основной сценарий
1. Пользователь формулирует задачу.
2. AI-компонент (`LLMHandler`) **интерпретирует суть задачи** — уточняет её при необходимости.
3. Менеджер (`ManagerHandler`) **определяет предполагаемую компетенцию**.
4. Если подходящий суб-бот найден:
   - Задача передаётся ему.
5. Если компетенция неизвестна:
   - Менеджер предлагает **делегировать фулстек-разработчику создание нового суб-бота**.
   - После создания — задача выполняется через нового бота.

## 🧠 Принципы
- Использование потенциала ИИ (через API).
- Умное делегирование задач.
- Создание суб-ботов по требованию.
- Масштабируемая архитектура без жёстких скриптов и тегов.

## 🤝 Суб-боты
- `DeveloperHandler` — фулстек и инфраструктура.
- `LawyerHandler` — юридические задачи.
- `WebResearcherHandler` — поиск информации.
- `LLMHandler` — универсальный fallback и интеллект-интерфейс.

## 🔁 Fallback-логика
- `ManagerHandler` теперь делегирует в `LLMHandler`, если не может найти исполнителя.
- `LLMHandler` может уточнить задачу, предложить план и сгенерировать команду.

## 🧱 Пример цепочки
