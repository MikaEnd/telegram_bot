# 📡 COMMUNICATION PROTOCOL

## Формат каждого изменения:
1. 💡 Сначала — краткое описание идеи: что и зачем.
2. 📝 Затем — команда для открытия файла: `nano путь/к/файлу`
3. 🔁 Далее — полное новое содержимое или текст для вставки.
4. 💾 После — `Ctrl+O`, `Enter`, `Ctrl+X`.
5. ⬆️ Наконец — `./sync_github.sh` с опцией Push.
6. ✅ Обязательно — тест задачи и лог результата в `HUMAN_EVOLUTION_LOG.txt`.

## Правила
- Всегда использовать `nano`, не править вручную.
- Всегда писать весь файл, без фрагментов (если не уточнено).
- Всегда тестировать перед фиксацией в GitHub.
- Стратегические изменения сопровождаются изменениями в `memory/`.

## Исключения
- `HUMAN_EVOLUTION_LOG.txt` и `DEV_PLAN.txt` дополняются в конец.
