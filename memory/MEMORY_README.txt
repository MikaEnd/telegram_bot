📄 MEMORY_README.txt — регламент работы с памятью проекта

🧠 Назначение  
Каталог `memory/` предназначен для хранения всей долгосрочной "памяти" проекта: стратегии, хронологии, архитектуры, протоколов общения и задач, а также инструкций по вводу новых ИИ-ассистентов. Эта папка критически важна для понимания контекста, соблюдения регламентов и обеспечения преемственности работы.

📥 Ввод нового ИИ-ассистента  
Перед началом работы любой новый ИИ обязан:

1. Прочитать содержимое `memory/prompt_for_new_assistant.md` — это стартовый файл, содержащий философию, цели, протокол общения и требования к работе ИИ в проекте.
2. Дождаться от пользователя команды `sync_github.sh` с выбором опции `2 — Push на GitHub`, выполненной вручную в терминале ВМ (Ubuntu) и подтверждённой в чате.
3. Обязательно запросить:
   - текущее локальное **время пользователя** (например, `2025-05-01 00:24`)
   - **часовой пояс** (например, `MSK (UTC+3)`)
   Это важно для синхронизации событий, логов и устранения путаницы в истории работы.

🔐 Ритуал разрешения на инспекцию  
ИИ не имеет права приступать к изучению содержимого папки `memory/` до получения специального разрешения от пользователя.

Обязательная фраза для разрешения:

✅ Разрешаю инспекцию контекста проекта

Только после получения этой фразы ассистент может приступить к анализу папки `memory/`.

📚 Структура текущих файлов в памяти

- `prompt_for_new_assistant.md` — стартовое руководство нового ассистента (обязательное к изучению)
- `DEV_PLAN.txt` — стратегический план и этапы проекта
- `HUMAN_EVOLUTION_LOG.txt` — хронология и важные вехи
- `ARCHITECTURE.md` — описание архитектуры системы и ролей ботов
- `BOT_COORDINATOR.md` — логика и принципы работы координатора задач
- `COMMUNICATION_PROTOCOL.txt` — правила общения и поведения в рамках проекта
- `AUDIT_CHECKLIST.txt` — список контрольных точек
- `INSPECTION_PROTOCOL.md` — детализированный регламент инспекции ассистентом
- `PROJECT_START_PROMPT.txt` — оригинальный сценарий начала проекта
- `PROMPT_GUIDELINE.md` — рекомендации по формированию промтов

🛠 Поддержание актуальности

После каждого важного изменения в проекте:

1. При добавлении нового файла в `memory/` — необходимо:
   - Внести его название и назначение в этот файл `MEMORY_README.txt`
   - Обновить `INSPECTION_PROTOCOL.md`, добавив требование изучить новый файл

2. Перед началом любой сессии работы:
   - Выполнить `sync_github.sh` с опцией `2 — Push на GitHub`
   - Убедиться, что все ассистенты используют актуальное состояние памяти

🤝 Заключение  
Папка `memory/` — это сердце управления проектом. ИИ не должен надеяться — он обязан знать, что работает с самой свежей и полной версией знаний. Любая халатность в инспекции и нарушении протокола — это угроза качеству и этике работы.
