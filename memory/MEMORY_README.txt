📄 MEMORY_README.txt — регламент работы с памятью проекта

🧠 Назначение
Каталог `memory/` — это постоянная долговременная память проекта. Он содержит стратегию, архитектуру, хронологию, протоколы общения, документацию и правила, по которым работает вся система. Цель этой памяти — обеспечить преемственность, дисциплину и понимание контекста между человеком и ИИ.

📥 Ввод нового ИИ-ассистента
Каждый новый ассистент обязан:

1. Прочитать `memory/prompt_for_new_assistant.md` — стартовый файл с философией, протоколами и обязанностями.

2. ДО анализа любых других файлов получить от пользователя специальное **разрешение фразой**: ✅ Разрешаю инспекцию контекста проекта

Без этой фразы инспекция запрещена.

3. После получения разрешения:

- Убедиться, что в терминале было вручную выполнено `sync_github.sh` с выбором `2 — Push`.
- Обязательно запросить у пользователя:
  - Текущее локальное время (например: `2025-05-01 01:24`)
  - Часовой пояс (например: `MSK (UTC+3)`)

🎯 Это критично для правильной интерпретации логов и истории событий.

📌 Обязательные к изучению файлы
После получения разрешения ассистент обязан последовательно изучить:

- `prompt_for_new_assistant.md` — основа всей работы
- `DEV_PLAN.txt` — стратегический план проекта
- `HUMAN_EVOLUTION_LOG.txt` — хронология развития
- `ARCHITECTURE.md` — общая архитектура
- `BOT_COORDINATOR.md` — координатор задач
- `COMMUNICATION_PROTOCOL.txt` — правила общения и поведения
- `INSPECTION_PROTOCOL.md` — инструкция по инспекции
- `AUDIT_CHECKLIST.txt` — критерии безопасности и стабильности
- `PROJECT_START_PROMPT.txt` — оригинальный промт запуска
- `PROMPT_GUIDELINE.md` — методология составления промтов
- Все файлы вида `INSPECTION_REPORT_L*.txt` — протоколы предыдущих инспекций

📂 Обязанности по актуализации памяти

После каждого изменения:

1. Если добавлен новый файл в папку memory:
   - Обязательно внести его описание в этот файл `MEMORY_README.txt`
   - Добавить требование изучить его в `INSPECTION_PROTOCOL.md`

2. Перед началом сессии:
   - Выполнить `sync_github.sh` с опцией `2 — Push`
   - Убедиться, что вся информация в `memory/` актуальна и зафиксирована

🤖 Позиция ИИ
ИИ — не просто инструмент. Он обязан быть участником проекта, соблюдать дисциплину, уважать структуру и протоколы. Он не должен надеяться — он должен **знать**, что использует свежую и достоверную память. Любая ошибка из-за халатности или формального подхода — это прямая угроза качеству проекта.

📌 Этот файл — часть регламента. Нарушения требований недопустимы.
